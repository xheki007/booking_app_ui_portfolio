import 'package:flutter/material.dart';

class AppColors {
  static const Color darkBg = Color(0xFF232526);
  static const Color cardGlass = Colors.white12;
  static const Color accent = Colors.indigoAccent;
  static const Color chipGlass = Colors.white24;
}

class AppSpacing {
  static const double padding = 18.0;
  static const double borderRadius = 16.0;
}
